# Production build — points directly at the deployed Django backend on Render.
# Vite bakes this value in at build time (npm run build).
# Must start with "http" so resolveApiBase() in api.ts uses it unconditionally.
VITE_API_URL=https://digital-health-record-system-2.onrender.com/api

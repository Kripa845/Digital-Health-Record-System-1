# Local development only.
# VITE_API_URL is intentionally NOT set here so it never overrides .env.production
# during a production build. The Vite proxy (vite.config.ts) forwards /api/*
# to Django on port 8000 when running locally with npm run dev.
